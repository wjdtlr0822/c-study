순수가상함수란?
class A{
    public:
    A(){}
    virtual ~A(){}
    virtual void speak()=0;
}

에서 virtual vodi speak()=0; 부분이 순수가상함수란

순수가상함수가 하나라도 포함된 클래스는 추상 클래스라고 부른다.

추상클래스는 객체를 생성 할 수 없고, 상속 받은 클래스를 만들어서 모든 순수가상함수를 오버라이딩 해주어야만 한다.


추상클래스의 특징
 : 객체를 바로 생성할 수 없다.
 : 추상 클래스를 가리키는 포인터는 문제 없이 만들 수 있다.